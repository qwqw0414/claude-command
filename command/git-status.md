# Git Status - 변경사항 요약

현재 Git 작업 상태를 분석하여 요약 정보를 제공합니다.

## 사용법

```bash
/git-status
```

## 실행 내용

1. **브랜치 정보**
   ```bash
   git branch --show-current
   git status
   ```

2. **변경사항 분류**
   - 스테이징된 파일
   - 수정된 파일 (미스테이징)
   - 추가되지 않은 새 파일
   - 삭제된 파일

3. **커밋 상태**
   - 현재 브랜치의 최신 커밋
   - origin과의 차이 (ahead/behind)
   - Unpushed 커밋 수

4. **작업 제안**
   - 다음에 할 작업 제안
   - 커밋 가능 여부
   - 충돌 경고

## 출력 예시

```
📍 현재 브랜치: feature/user-auth
🔄 origin/feature/user-auth와 동기화됨

📊 변경사항 요약:

✅ 스테이징됨 (3):
  M  src/auth/login.ts
  A  src/auth/token.ts
  M  tests/auth.test.ts

⚠️  수정됨 (2):
  M  src/api/user.ts
  M  README.md

❓ 추적되지 않음 (1):
  ?? src/auth/refresh.ts

📝 최근 커밋:
  abc1234 - feat(auth): Add basic authentication (2 hours ago)

💡 다음 작업 제안:
  1. 스테이징된 파일이 있습니다 → /auto-commit 실행 권장
  2. 수정된 파일 검토 → git add <file> 또는 /split-commit
  3. 추적되지 않은 파일 → git add src/auth/refresh.ts

✨ 빠른 명령어:
  /auto-commit          - 스테이징된 파일 커밋
  /split-commit         - 변경사항 분리하여 커밋
  /commit-history       - 커밋 히스토리 분석
```

## 관련 명령어

- `/auto-commit` - 자동 커밋 메시지 생성
- `/split-commit` - 커밋 분리
- `/commit-history` - 커밋 히스토리 분석

